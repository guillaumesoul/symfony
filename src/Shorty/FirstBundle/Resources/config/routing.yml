first_homepage:
    pattern:  /hello/{name}
    defaults: { _controller: FirstBundle:Default:index }

test:
    pattern: /test
    defaults: { _controller: FirstBundle:Default:test }

form:
    pattern: /form
    defaults: { _controller: FirstBundle:Default:form }

task_success:
  pattern: /task_success
  defaults: { _controller: FirstBundle:Default:success }